<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pageModels="clr-namespace:FoodStreet.PageModels"
    xmlns:models="clr-namespace:FoodStreet.Models"

    x:Class="FoodStreet.Pages.MainPage"
    Title="Danh sách điểm thuyết minh">

    <VerticalStackLayout Padding="20" Spacing="10">

    

        <!-- Thanh tìm kiếm -->
        <SearchBar
            Placeholder="Tìm quán...."
            TextChanged="OnSearchTextChanged"/>

        <CollectionView ItemsSource="{Binding Pois}">

            <CollectionView.ItemTemplate>
                <DataTemplate>

                    <Border Padding="10" Margin="0,5">

                        <VerticalStackLayout>

                            <Label
                        Text="{Binding Name}"
                        FontAttributes="Bold"/>

                            <Label
                        Text="{Binding Description}"
                        FontSize="12"/>

                            <!-- LINK MAP -->
                            <Label
                        Text="Xem trên bản đồ"
                        TextColor="Blue">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                Tapped="OnOpenMap"
                                CommandParameter="{Binding .}" />
                                </Label.GestureRecognizers>

                            </Label>

                        </VerticalStackLayout>

                    </Border>

                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
    </VerticalStackLayout>

</ContentPage>